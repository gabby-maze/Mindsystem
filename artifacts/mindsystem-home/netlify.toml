[build]
  command   = "npm install --legacy-peer-deps && npm run build"
  publish   = "dist/public"

[build.environment]
  BASE_PATH = "/"
  PORT      = "3000"
  NODE_VERSION = "20"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
